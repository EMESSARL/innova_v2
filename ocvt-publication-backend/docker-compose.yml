services:
  ocvt-pub-module:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "$SERVER_PORT:$SERVER_PORT"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
